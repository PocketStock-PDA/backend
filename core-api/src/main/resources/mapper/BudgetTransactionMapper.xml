<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pocketstock.core.budget.mapper.BudgetTransactionMapper">

    <select id="findTransactions" resultType="com.pocketstock.core.budget.dto.TransactionRow">
        SELECT
            id AS transactionId,
            category AS category,
            merchant_name AS description,
            amount AS amount,
            paid_at AS transactedAt
        FROM card_transactions
        WHERE user_id = #{userId}
          AND is_cancelled = false
          <if test="from != null">AND paid_at &gt;= #{from}</if>
          <if test="to != null">AND paid_at &lt;  #{to}</if>
        ORDER BY paid_at DESC
    </select>

</mapper>
