<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pocketstock.core.asset.mapper.SpendingMapper">

    <select id="findCategorySpending" resultType="com.pocketstock.core.asset.dto.CategoryAmountRow">
        SELECT category, SUM(amount) AS amount
        FROM card_transactions
        WHERE user_id = #{userId}
          AND is_cancelled = false
          <if test="from != null">AND paid_at &gt;= #{from}</if>
          <if test="to != null">AND paid_at &lt;  #{to}</if>
          AND paid_at &lt;= NOW()   <!-- 미래(현재시각 이후) 거래 제외 -->
        GROUP BY category
        ORDER BY amount DESC
    </select>

</mapper>
