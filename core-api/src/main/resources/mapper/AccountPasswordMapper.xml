<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace = 인터페이스 풀패키지명(user 모듈)과 정확히 일치 -->
<mapper namespace="com.pocketstock.user.member.mapper.AccountPasswordMapper">

    <select id="findByUserId" resultType="com.pocketstock.user.member.domain.AccountPassword">
        SELECT id, user_id, password_hash, is_locked
        FROM account_passwords
        WHERE user_id = #{userId}
    </select>

    <!-- user_id UNIQUE — 최초 설정은 INSERT, 재설정은 password_hash UPDATE -->
    <insert id="upsert" parameterType="com.pocketstock.user.member.domain.AccountPassword">
        INSERT INTO account_passwords (user_id, password_hash)
        VALUES (#{userId}, #{passwordHash})
        ON DUPLICATE KEY UPDATE password_hash = #{passwordHash}
    </insert>

</mapper>
