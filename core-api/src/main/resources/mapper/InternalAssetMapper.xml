<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pocketstock.core.internal.asset.mapper.InternalAssetMapper">

    <select id="findLinkedAccountsByUserAndIds"
            resultType="com.pocketstock.core.internal.asset.dto.LinkedAccountSummary">
        <!-- maturityAmount = 원금 + 만기 단리 이자(예적금만; 그 외 = balance). 만기 굴리기 집행 한도 검증용.
             적금(SAVINGS)은 매달 납입이라 (개월+1)/24, 예금(DEPOSIT)은 개월/12. -->
        SELECT id, account_type, balance, currency, maturity_date,
               balance + CASE
                   WHEN interest_rate IS NULL OR maturity_date IS NULL THEN 0
                   WHEN account_type = 'SAVINGS'
                   THEN ROUND(balance * interest_rate
                              * (GREATEST(COALESCE(TIMESTAMPDIFF(MONTH, start_date, maturity_date), 12), 1) + 1) / 24)
                   ELSE ROUND(balance * interest_rate
                              * GREATEST(COALESCE(TIMESTAMPDIFF(MONTH, start_date, maturity_date), 12), 1) / 12)
                   END AS maturityAmount
        FROM linked_bank_accounts
        WHERE user_id = #{userId}
          AND closed_at IS NULL
          AND id IN
          <foreach item="id" collection="ids" open="(" separator="," close=")">
              #{id}
          </foreach>
    </select>

    <!-- 외화(USD) 입출금 지갑 목록 — 잔돈 수집의 FX 소스(전액 입금 대상). scan(sumUsdWalletBalance)과 동일 모집단.
         account_type='DEMAND'(입출금식 외화지갑)만 포함 — 외화 정기예금/적금(DEPOSIT·SAVINGS)은 잔돈이 아니라 제외.
         id+이름+잔액을 반환해 홈에 계좌 단위로 노출하고, 수집 후 deductAccountBalance로 원천을 닫는다.
         컬럼 순서는 UsdWalletSummary 레코드 구성요소(id, accountName, balance, currency)와 일치시킨다. -->
    <select id="findUsdWallets"
            resultType="com.pocketstock.core.internal.asset.dto.UsdWalletSummary">
        SELECT id, account_name, balance, currency
        FROM linked_bank_accounts
        WHERE user_id = #{userId}
          AND currency = 'USD'
          AND account_type = 'DEMAND'
          AND is_dormant = FALSE
          AND closed_at IS NULL
    </select>

    <!-- is_roundup_collected=FALSE, 미취소 건만 조회. card_id = linked_cards.id (구 linked_account_id) -->
    <select id="findUncollectedCardTxs" resultType="map">
        SELECT id, amount
        FROM card_transactions
        WHERE user_id = #{userId}
          AND card_id = #{linkedAccountId}
          AND is_roundup_collected = FALSE
          AND is_cancelled = FALSE
    </select>

    <!-- 수집 확정 시 실현 라운드업(천원 올림 차액)을 함께 기록(E-3) — 시드의 "수집된 행=roundup_amount 채움" 컨벤션과 일치 -->
    <update id="markRoundupCollected">
        UPDATE card_transactions
        SET is_roundup_collected = TRUE,
            roundup_collected_at = NOW(),
            roundup_amount = CEIL(amount / 1000) * 1000 - amount
        WHERE user_id = #{userId}
          AND is_roundup_collected = FALSE
          AND id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 연동 포인트 잔액 조회. linked_points는 포인트 전용 테이블(balance BIGINT, 1P=1원) -->
    <select id="findPointBalance" resultType="java.math.BigDecimal">
        SELECT balance
        FROM linked_points
        WHERE user_id = #{userId}
          AND id = #{linkedAccountId}
    </select>

    <!-- 포인트명 — 신한/제휴 분류·표시용 -->
    <select id="findPointName" resultType="java.lang.String">
        SELECT point_name
        FROM linked_points
        WHERE user_id = #{userId}
          AND id = #{linkedAccountId}
    </select>

    <!-- 끝전 수집 확정: 연동 계좌 잔액에서 수집액 차감 → 잔액이 threshold 배수가 되어 다음 끝전=0.
         balance >= amount 가드로 음수 잔액을 DB 레벨에서 차단(조건 불충족 시 0건 → 서비스가 예외 처리) -->
    <update id="deductAccountBalance">
        UPDATE linked_bank_accounts
        SET balance = balance - #{amount}
        WHERE user_id = #{userId}
          AND id = #{id}
          AND closed_at IS NULL
          AND balance &gt;= #{amount}
    </update>

    <!-- 포인트 수집 확정: 연동 포인트 잔액에서 수집액 차감(전환분만큼). balance >= amount 가드로 음수 차단 -->
    <update id="deductPointBalance">
        UPDATE linked_points
        SET balance = balance - #{amount}
        WHERE user_id = #{userId}
          AND id = #{id}
          AND balance &gt;= #{amount}
    </update>

</mapper>
