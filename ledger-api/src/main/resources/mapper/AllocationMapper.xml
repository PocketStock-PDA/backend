<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pocketstock.ledger.trading.mapper.AllocationMapper">

    <insert id="insert" parameterType="com.pocketstock.ledger.trading.domain.Allocation"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO allocations
            (order_id, batch_order_id, allocated_qty, allocated_price, gross_amount, fee, tax, net_amount)
        VALUES
            (#{orderId}, #{batchOrderId}, #{allocatedQty}, #{allocatedPrice}, #{grossAmount},
             #{fee}, #{tax}, #{netAmount})
    </insert>

</mapper>
