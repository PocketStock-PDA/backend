<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pocketstock.ledger.cma.mapper.CollectionSettingMapper">

    <resultMap id="collectionSetting" type="com.pocketstock.ledger.cma.domain.CollectionSetting">
        <id     property="id"          column="id"/>
        <result property="userId"      column="user_id"/>
        <result property="sourceType"  column="source_type"/>
        <result property="sourceRefId" column="source_ref_id"/>
        <result property="isEnabled"   column="is_enabled"/>
        <result property="threshold"   column="threshold"/>
        <result property="createdAt"   column="created_at"/>
        <result property="updatedAt"   column="updated_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="collectionSetting">
        SELECT id, user_id, source_type, source_ref_id, is_enabled, threshold, created_at, updated_at
        FROM collection_settings
        WHERE user_id = #{userId}
    </select>

    <!-- UNIQUE KEY (user_id, source_type, source_ref_id) 기준 upsert -->
    <insert id="upsert" parameterType="com.pocketstock.ledger.cma.domain.CollectionSetting">
        INSERT INTO collection_settings (user_id, source_type, source_ref_id, is_enabled, threshold)
        VALUES (#{userId}, #{sourceType}, #{sourceRefId}, #{isEnabled}, #{threshold})
        ON DUPLICATE KEY UPDATE
            is_enabled = #{isEnabled},
            threshold  = #{threshold},
            updated_at = NOW()
    </insert>

    <!-- 소스타입 전체(예: CARD 다중 카드) 활성 일괄 변경 — 마이페이지 마스터 토글. -->
    <update id="updateEnabledBySourceType">
        UPDATE collection_settings
        SET is_enabled = #{enabled},
            updated_at = NOW()
        WHERE user_id     = #{userId}
          AND source_type = #{sourceType}
    </update>

</mapper>
