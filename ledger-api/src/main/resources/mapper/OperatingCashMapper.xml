<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pocketstock.ledger.trading.mapper.OperatingCashMapper">

    <!-- 회사 현금 잔액 원자 upsert: balance += delta. 통화행 없으면 INSERT(첫 거래 self-seeding).
         ON DUPLICATE KEY UPDATE의 원자 증감이 lost update를 막음(C1과 동형).
         유저 예수금과 달리 음수 가드 없음 — 회사 순현금은 음수가 될 수 있음. -->
    <update id="applyDelta">
        INSERT INTO operating_cash_balances (currency, balance)
        VALUES (#{currency}, #{delta})
        ON DUPLICATE KEY UPDATE balance = balance + #{delta}
    </update>

    <select id="findBalanceByCurrency" resultType="java.math.BigDecimal">
        SELECT balance
        FROM operating_cash_balances
        WHERE currency = #{currency}
    </select>

    <insert id="insert" parameterType="com.pocketstock.ledger.trading.domain.OperatingCashTransaction"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operating_cash_transactions
            (currency, tx_type, amount, balance_after, ref_type, ref_id, idempotency_key)
        VALUES
            (#{currency}, #{txType}, #{amount}, #{balanceAfter}, #{refType}, #{refId}, #{idempotencyKey})
    </insert>

</mapper>
