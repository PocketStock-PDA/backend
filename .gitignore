# Gradle
.gradle/
**/build/
!gradle/wrapper/gradle-wrapper.jar

# IntelliJ IDEA
.idea/
*.iml
out/

# macOS
.DS_Store

# 환경변수 / 시크릿 (커밋 금지)
.env
**/application-local.yml
**/application-secret.yml

# 무중단 배포(Blue-Green) 런타임 상태 — deploy-release.sh 가 활성 색으로 생성(추적 금지)
deploy/nginx/conf.d/active-upstream.conf

# 로그
*.log
logs/

# 개인 참고용 md 폴더
docs/un6unw

# Claude (로컬 전용)
CLAUDE.md
