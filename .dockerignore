# Docker 빌드 컨텍스트 제외 — 빌드 빠르게 + 시크릿/쓰레기 이미지 유입 차단
.git
.gradle
**/build
# 단, CI/CD 가 미리 빌드한 실행 jar 는 이미지에 COPY 해야 하므로 예외로 포함 (BuildKit 재포함)
!core-api/build/libs/*.jar
!ledger-api/build/libs/*.jar
**/application-local.yml
**/application-secret.yml
.env
deploy/compose/.env
docs
*.log
.idea
*.iml
